
#include "parser.h"

std::map<keys, std::string> parseConfigFile(std::map<keys, std::string> cmdLineArgs) {
	std::map<keys, std::string> map = cmdLineArgs;

//	rude::Config config;
	

//	bool loadStatus = config.load(map[configFile].c_str());
//	if(!loadStatus) {
//		std::cout << "error" << std::endl;
//	}

}
